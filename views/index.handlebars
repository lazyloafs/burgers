<h1>Burgers!</h1>

<h2>Burgers that are yummy!</h2>

<ul>
  {{#each burger}}
    {{#unless yummy}}
      {{> burger/burger-block yummy=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are Yummy!</h2>

<ul>
  {{#each burger}}
    {{#if yummy}}
      {{> burger/burger-block eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Yummy?</label><br>
    <input type="radio" name="yummy" value="1" checked> Yummy!<br>
    <input type="radio" name="yummy" value="0"> Eaten
  </div>

  <button type="submit">Add Burger</button>
</form>
